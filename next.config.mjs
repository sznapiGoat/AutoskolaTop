/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "390ce95b86.clvaw-cdnwnd.com",
      },
    ],
  },
};

export default nextConfig;
